<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Guia pergunstas</title>
    <link rel="stylesheet" href="/css/style.css">
</head>
<body>
        <h1>Guia pergustas!</h1>
        <hr>
        <h3>Lista de pergunstas</h3>

        <p>Nome:<%= nome %></p>
        <p>Linguagem Favorita:<%= lang %></p>
        <p>Empresa: <%= Empresa %></p>
        <p>Inscrito no Canal:<%= inscritos %></p>

        <% if(msg == true){ %>
             <h3> Isso é uma msg de Erro!</h3>
        <%} else { %>
                <h3>Nenhum erro!</h3>
        <%}%> 
        
        <% produto.forEach(function(produto){ %>
            <hr>
            <h3><%= produto.nome %></h3> 
            <h4> <%= produto.preco %></h4>
        <% })%>
</body> 
</html>